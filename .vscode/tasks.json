{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Host tests",
      "type": "shell",
      "command": "cmake --preset host-tests && cmake --build --preset host-tests && ctest --test-dir build-host --output-on-failure",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "test",
      "problemMatcher": ["$gcc"]
    }
  ]
}
